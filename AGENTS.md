# RailShot AI 開発ガイド

このファイルはリポジトリ全体に適用する。実装時は `docs/SPEC.md` と `docs/IMPLEMENTATION_PLAN.md` も読み、指示が競合する場合はユーザーの最新の明示指示を優先する。

## プロジェクト方針

- このプロジェクトは RailShot AI という鉄道撮影支援AIである。
- ハッカソン向けMVPであり、機能数より3分間のデモが確実に動くことを優先する。
- 「3分デモ」は審査時に人間が実際のアプリを操作して行う発表を指す。デモ専用モード、録画機能、自動再生機能、偽データによるデモ演出などを実装する必要はない。
- 全国対応を最初から目指さない。まず1路線、5駅以上、10列車以上の検証済みデモデータで完成させる。
- TypeScript と Next.js を使用する。
- 地図は MapLibre GL JS を基本とし、地理データの共通形式は GeoJSON とする。
- 鉄道検索、営業日・時刻処理、通過時刻補間、距離計算、日付判定、光線計算、ランキングは、LLMではなくテスト可能な決定的ロジックで実装する。
- AIは自然言語の構造化と、必要に応じた推薦理由の生成だけに使用する。
- 確定情報、予定、推定、不明を区別する。駅間通過時刻には必ず「推定」または「ごろ」と表示する。
- 撮影地点の推薦対象は、安全を確認して事前登録した地点だけとする。私有地、線路内、立入禁止区域、危険箇所を推薦しない。
- 外部の商用時刻表サイト等を無断スクレイピングしない。利用条件を確認したGTFS、またはハッカソン用デモGTFSを使用する。

## アーキテクチャ方針

- Next.js App Routerを使用し、画面とRoute Handlerを同一プロジェクトに置く。
- ブラウザ、HTTP API、ドメインロジック、データアクセス、外部LLM連携の責務を分離する。
- ドメインロジックはフレームワーク、SQLite、LLMから独立した純粋関数を優先し、単体テスト可能にする。
- GTFS時刻は `Date` だけで表現せず、営業日の0:00からの経過秒数として保持し、24時を超える値を扱う。
- SQLはプレースホルダー付きクエリを使い、入力を文字列連結しない。
- API境界、外部データ、LLM出力はZod等で検証する。
- MapLibre GL JSを使うUIはClient Componentに限定し、鉄道検索と秘密情報を扱う処理はサーバー側に置く。
- ハッカソン中の主要鉄道データはローカルSQLiteから取得し、ネットワーク障害時もP0機能を動作可能にする。

## LLM基盤

このプロジェクトでは、アプリ内のLLM呼び出しに OrcaRouter を使用する。OrcaRouterはOpenAI互換APIとして利用する。

- Base URL: `https://api.orcarouter.ai/v1`
- Model: `orcarouter/auto`
- API key environment variable: `ORCAROUTER_API_KEY`

OpenAI APIを直接呼び出す実装を基本形にしない。OpenAI互換クライアントを使う場合も、サーバー側で上記Base URLとモデルを明示し、経路を次に固定する。

```text
RailShot AI
  -> サーバー側API
  -> OrcaRouter
  -> OrcaRouterが選択したLLM
```

LLMプロバイダー固有処理はサーバー専用の単一アダプターに隔離し、クライアントやドメイン層から直接呼ばない。

## セキュリティ

- `ORCAROUTER_API_KEY` をクライアントへ露出させない。`NEXT_PUBLIC_` を付けず、Client Component、ブラウザレスポンス、ログへ渡さない。
- APIキーは `.env.local` 等の環境変数で管理し、秘密値を含む `.env` 系ファイルをGitへコミットしない。共有用には値を含まない `.env.example` だけを例外として使う。
- LLMへの入力は必要最小限にし、秘密情報、不要な個人情報、データベース全体を送信しない。
- LLM出力はZod等でスキーマ検証し、失敗時は安全にエラーとして扱う。
- LLM出力をそのままSQL、内部処理、ファイルパス、HTMLへ渡さない。検証・正規化した許可済みフィールドだけを使用する。
- AIに任意のOSコマンドを実行させない。
- 外部時刻表サイトを無断スクレイピングしない。
- API入力にはサイズ上限とスキーマ検証を設け、エラーを秘密情報なしで返す。
- 撮影地点は安全確認済みデータに限定し、安全上の注意事項をUIで欠落させない。

## LLMコスト

鉄道検索、時刻計算、距離計算、日付判定、通過時刻補間、光線計算、ランキング等にLLMを利用しない。

LLMを利用するのは原則として次の2箇所だけとする。

1. 自然言語から構造化検索条件への変換
2. 必要な場合の、決定的ロジックが返した事実に基づく推薦理由生成

- 同一の正規化済み検索条件はキャッシュ可能な境界を設ける。
- 1回の操作で不要な複数回呼び出しを行わない。推薦理由はテンプレートで十分ならLLMを呼ばない。
- OrcaRouterのリクエストログを、ハッカソン審査でLLM実測コストを説明するために利用できる構造にする。
- アプリ側には少なくとも用途、リクエストID、日時、選択モデル、レイテンシ、入力/出力トークン数、キャッシュ結果、成功/失敗を構造化記録する。プロンプト本文やAPIキーは記録しない。
- 利用量やコスト情報がレスポンスに存在しない場合は推測値を実測値として保存せず、OrcaRouter側ログと照合可能なIDを保持する。

## 開発ルール

- 大規模なリファクタリングを勝手に行わない。
- 既存機能を壊さない。変更前に影響範囲と既存テストを確認する。
- 指定されたフェーズより先の機能を勝手に実装しない。P0/P1の安定性をP2/P3より優先する。
- 各タスク終了時に、変更範囲に応じた自動テスト、型チェック、lintのいずれか、可能ならすべてを実施する。
- エラーを隠さない。握りつぶさず、利用者向け状態と調査可能なサーバーログを分ける。
- 変更内容、確認方法、未解決事項を最後に報告する。
- 依存パッケージや外部サービスを追加する際は、目的、ライセンス、デモ時の可用性を確認する。
- 生成物、SQLiteの一時ファイル、秘密情報をGit管理へ入れない。
- 仕様変更が必要な場合は、実装で黙って迂回せず `docs/SPEC.md` または計画との差分を報告する。

## 必須テスト観点

- GTFS時刻: `12:00`、`23:59`、`24:30`、`25:10`。
- 距離補間: 駅直上、駅間25%・50%・75%、次駅直前、ゼロ距離や不正shape。
- 営業日: 通常カレンダー、追加日、除外日、日付またぎ。
- 列車検索: 時間内、時間外、0件、複数件、時刻順。
- AI構造化: 正常出力、不正JSON、スキーマ違反、範囲外値、余分な指示への耐性。
- API/UI: Loading、Error、空結果を明示し、LLM障害時にも決定的な鉄道検索が壊れないこと。
